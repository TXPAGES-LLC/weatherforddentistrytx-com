/**
 * Single source of truth for the canonical production URL.
 * Import SITE_URL everywhere a base URL is needed — sitemap, robots,
 * metadata canonicals, OG tags, structured data — so deployment
 * environment changes never cause URL drift.
 */
export const SITE_URL = 'https://weatherforddentistrytx.com'
